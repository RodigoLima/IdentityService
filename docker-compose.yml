version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: identityservice-api
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Host=host.docker.internal;Port=5432;Database=IdentityService;Username=docker;Password=docker
      - Jwt__Key=7G+H65bLToXxqzPvj7+q0oQUlxJp1WvdOU3nv3ArA1s=
      - Jwt__ExpirationTimeHour=5
      - Jwt__IncreaseExpirationTimeMinutes=20
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
    networks:
      - identityservice-network

  prometheus:
    image: prom/prometheus:latest
    container_name: identityservice-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheus/consoles'
    depends_on:
      - api
    restart: unless-stopped
    networks:
      - identityservice-network

  grafana:
    image: grafana/grafana:latest
    container_name: identityservice-grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_USERS_ALLOW_SIGN_UP=false
    volumes:
      - grafana_data:/var/lib/grafana
      - ./monitoring/grafana/datasources.yml:/etc/grafana/provisioning/datasources/datasources.yml
      - ./monitoring/grafana/dashboards.yml:/etc/grafana/provisioning/dashboards/dashboards.yml
      - ./monitoring/grafana/identityservice-dashboard.json:/etc/grafana/provisioning/dashboards/identityservice-dashboard.json
    depends_on:
      - prometheus
    restart: unless-stopped
    networks:
      - identityservice-network

volumes:
  prometheus_data:
    driver: local
  grafana_data:
    driver: local

networks:
  identityservice-network:
    driver: bridge
