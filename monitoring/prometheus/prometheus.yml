global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'identityservice-monitor'

scrape_configs:
  - job_name: 'identityservice-api'
    static_configs:
      - targets: ['api:8080']
        labels:
          application: 'identityservice-api'
          environment: 'development'
    
    metrics_path: '/metrics'
    scrape_interval: 10s
    scrape_timeout: 5s
